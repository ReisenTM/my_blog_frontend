设计系统为 **React + TypeScript** 架构，确保组件层面的**类型安全**、**可维护性**和**原子性**。

以下UI风格的编程参考文档。

---

### **React/TS 编程参考文档：'代码的秩序美学' 设计系统 (Code Orderliness)**

**目标：** 将设计令牌 (Design Tokens) 转化为可导入的 TypeScript 常量和类型定义，并规范关键 React 组件的结构和接口 (Props)。

---

### 1. 设计令牌模块化 (Design Tokens Module)

**指令：** AI Agent 应创建并导出以下模块，作为所有组件的单一事实来源 (Single Source of Truth)。

#### 1.1 `src/styles/colors.ts` (配色方案)

TypeScript

```
// 定义颜色常量
export const Colors = {
    // Backgrounds
    bgMain: '#FFFFFF',
    bgSecondary: '#F4F4F7',
    bgInlineCode: '#E6F0FF',
    codeBg: '#2D2D2D',

    // Text & Primary
    textMain: '#202020',
    textSecondary: '#606060',
    
    // Accents & Functionality
    accentBlue: '#007AFF',
    textCodeInline: '#D90429',
    borderColor: '#E0E0E0',

    // Alerts
    tipBorder: '#007AFF',
    tipBg: '#F0F7FF',
    warningBorder: '#FFC107',
    warningBg: '#FFF9E6',
};
```

#### 1.2 `src/styles/typography.ts` (字体系统)

TypeScript

```
// 定义字体家族常量
export const Fonts = {
    fontUi: "'Inter', 'Helvetica Neue', sans-serif",
    fontBody: "'Lora', 'Georgia', 'Songti SC', serif",
    fontCode: "'JetBrains Mono', 'Fira Code', monospace",
};

// 定义基础字号 (可根据需要扩展)
export const FontSizes = {
    h1: '38px',
    h2: '28px',
    body: '17px',
    small: '14px',
    code: '15px',
};
```

#### 1.3 **CSS 变量注入 (Global Styling)**

**指令：** 建议在应用的顶层组件或全局 CSS 文件中，通过这些 TS 常量来定义 CSS 变量，以便于组件访问和主题切换。

CSS

```
/* src/styles/global.css 或 ThemeProvider.ts */
:root {
    /* 注入颜色变量 */
    --color-bg-main: #FFFFFF; /* 映射 Colors.bgMain */
    --color-accent-blue: #007AFF; /* 映射 Colors.accentBlue */
    
    /* 注入字体变量 */
    --font-family-body: 'Lora', serif; /* 映射 Fonts.fontBody */
    --font-family-code: 'JetBrains Mono', monospace; /* 映射 Fonts.fontCode */
}
```

---

### 2. 关键组件规范 (Component Specification)

**指令：** AI Agent 必须使用以下组件结构和接口 (Props) 来构建页面。所有组件都应使用语义化标签。

#### 2.1 `<PostLayout />` (文章三栏布局)

|**Prop**|**Type**|**描述**|**强制**|
|---|---|---|---|
|`children`|`React.ReactNode`|文章正文内容 (`<article>`)|是|
|`toc`|`React.ReactNode`|左侧目录 (`<TableOfContents>`)|是|
|`sidebar`|`React.ReactNode`|右侧侧边栏 (`<aside>`)|否|

**样式规则：**

- 使用 **CSS Grid** (`grid-template-columns: 240px 1fr 240px;`) 实现三栏结构。
    
- 中心内容栏 (`1fr`) 应设置 `max-width: 800px` 限制阅读宽度。
    
- 应用 **响应式规则** (见下文 3.1)。
    

#### 2.2 `<TableOfContents />` (左侧目录)

|**Prop**|**Type**|**描述**|**强制**|
|---|---|---|---|
|`headings`|`Array<{id: string; text: string; level: number}>`|标题列表数据|是|
|`activeId`|`string`|当前滚动到的标题 ID|否|

**样式规则：**

- 容器必须使用 **`position: sticky`** 和 `top: 20px`。
    
- 背景色：`--color-bg-secondary`。
    
- 当前激活链接 (`activeId` 对应项) 必须应用 **`--color-accent-blue`**。
    

#### 2.3 `<CodeBlock />` (代码块)

**这是核心技术组件，需要内部封装复制逻辑。**

|**Prop**|**Type**|**描述**|**强制**|
|---|---|---|---|
|`language`|`string`|编程语言 (如 'python', 'typescript')|是|
|`content`|`string`|原始代码内容|是|

**样式规则：**

- `<pre>` 容器：
    
    - `background-color: var(--code-bg);`
        
    - `font-family: var(--font-code);`
        
    - **必须**在右上角渲染 **语言标签** 和 **“复制”按钮**。
        
- **功能实现：** 内部逻辑应处理 `content` 的 **语法高亮** (推荐使用如 Prism.js 或 React Syntax Highlighter 库)。
    

#### 2.4 `<Alert />` (提示/警告框)

TypeScript

```
// 定义 Alert 组件的类型
export type AlertType = 'tip' | 'warning';

// Alert 组件的 Props
export interface AlertProps {
    type: AlertType;
    children: React.ReactNode;
}
```

**样式规则：**

- 容器 **禁止** 使用默认的 `<blockquote>` 样式。
    
- 必须有 `5px` 宽度的左侧边框。
    
    - `type="tip"`: 使用 `var(--tip-border)` 和 `var(--tip-bg)`。
        
    - `type="warning"`: 使用 `var(--warning-border)` 和 `var(--warning-bg)`。
        
- 内容必须以相应的图标和文本开头 (如 `💡 提示：`)。
    

---

### 3. 响应式与行为规范

#### 3.1 响应式断点 (Breakpoints)

|**视口尺寸**|**行为**|**规则**|
|---|---|---|
|**Desktop** (`> 1200px`)|三栏全显示|`grid-template-columns: 240px 1fr 240px;`|
|**Tablet** (`769px - 1200px`)|两栏显示|隐藏 `.post-sidebar`，`grid-template-columns: 200px 1fr;`|
|**Mobile** (`<= 768px`)|单栏显示|隐藏 `.post-toc`，`grid-template-columns: 1fr;`|

#### 3.2 交互行为

- **链接：** 使用 `var(--accent-blue)` 作为 `hover` 颜色。
    
- **TOC：** 激活项应通过监听滚动事件，动态更新 `activeId`，并应用蓝色高亮。